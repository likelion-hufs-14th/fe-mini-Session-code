/** Fire 이글이글 애니메이션(GSAP tween 동작) 확인용 스토리. */
import Fire from './Fire';

/** Fire 스토리 메타. */
export default {
  title: 'fire/Fire',
  component: Fire,
};

/** props 없음 — 기본 렌더링과 넘실거림만 확인. */
export const Default = {};
